{
  "name": "wolfdev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && cp -r media .next/standalone/ && mkdir -p .next/standalone/public && cp -r public/content .next/standalone/public/ && cp -r .next/static .next/standalone/.next/ && rm -rf next-app && cp -r .next/standalone next-app && rm -rf next-app/node_modules",
    "standalone": "node .next/standalone/server.js",
    "lint": "next lint",
    "generate-types": "quicktype public/content/content.json -o src/lib/contentTypes.ts --just-types"
  },
  "dependencies": {
    "mime-types": "^3.0.1",
    "next": "15.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/mime-types": "^2.1.4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.2.4",
    "typescript": "^5"
  }
}
